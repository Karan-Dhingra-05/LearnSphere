# Read by `docker compose build` to bake the backend's browser-facing URL
# into the frontend bundle. Must match the server's published port/host
# below (docker-compose.yml maps the server to host port 5002).
VITE_API_URL=http://localhost:5002/api
